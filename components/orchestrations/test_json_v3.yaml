name: test_json_v3
component_type: orchestration
version: 1.0.0
description: Test JSON emission with variation 3 (explicit instructions)
author: claude
agents:
  tester:
    component: components/behaviors/testing/json_test_v3_explicit
    prompt: 'Please emit this JSON event:

      {"event": "orchestration:track", "data": {"variation": "v3", "test": "explicit"}}

      '
metadata:
  purpose: manual_mipro_testing
