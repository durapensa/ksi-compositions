name: test_json_v1
type: orchestration
version: 1.0.0
description: Test JSON emission with variation 1 (simple instructions)
author: claude

agents:
  tester:
    profile: json_test_v1_simple
    vars:
      prompt: |
        Please emit this JSON event:
        {"event": "orchestration:track", "data": {"variation": "v1", "test": "simple"}}

metadata:
  purpose: manual_mipro_testing