name: test_creation_workflow
type: prompt
version: 1.0.0
description: Testing the complete prompt creation workflow
author: test_user
category: test

content: |
  This is a test prompt to understand the composition creation workflow.
  
  It demonstrates:
  1. How prompts are created and saved
  2. How the index is automatically updated
  3. The proper structure for prompt compositions
  
  This prompt can be composed using:
  ksi send composition:prompt --name test_creation_workflow

metadata:
  tags: [test, workflow, creation]
  purpose: understanding_composition_system